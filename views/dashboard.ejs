<%- include('layout', { body: `
<h1>Dashboard</h1>
<div class="cards">
  <div class="card" id="totalCodes">Total de códigos: ...</div>
  <div class="card" id="availableCodes">Disponíveis: ...</div>
  <div class="card" id="usedCodes">Usados: ...</div>
  <div class="card" id="todayCodes">Entregues hoje: ...</div>
  <div class="card" id="totalBatches">Total de lotes: ...</div>
</div>

<h2>Últimos códigos utilizados</h2>
<table>
  <thead>
    <tr><th>Código</th><th>Data</th></tr>
  </thead>
  <tbody id="lastUsedTable">
    <tr><td colspan="2">Carregando...</td></tr>
  </tbody>
</table>
<script src="/js/dashboard.js"></script>
` }) %>
